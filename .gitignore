# Local reference material — not for the repo
temp/

# Implementation plans are kept local-only (specs are public)
docs/superpowers/plans/

# macOS
.DS_Store

# Local-only secrets / per-machine config
# (Gradle will read postcode etc. from here once the Android project is scaffolded)
local.properties

# Gradle build outputs and caches
.gradle/
app/build/
build/
